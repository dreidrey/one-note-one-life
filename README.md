# One Note, One Life

Write once. Read once. Gone forever.

A self destructing note app that you can share with others.

## Run locally

```bash
pip install fastapi uvicorn
uvicorn main:app --reload
```

Open `http://localhost:8000`.